# Datos del panel de administración: contraseña, contenido, versiones,
# mensajes del formulario. NADA de esta carpeta debe servirse por HTTP.
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order deny,allow
  Deny from all
</IfModule>
